export const screenSizeWidth = window.innerWidth;
export const screenSizeHeight = window.innerHeight;

const size = {
   desktop: '1440px',
   tablet: '768px',
   mobile: '375px'
}

export const device = {
  desktop: `max-width`
}